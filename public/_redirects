# Redirects — Cloudflare Workers Static Assets
# Traduzido do netlify.toml em 18/09/2026.

# Atalhos para divulgar na bio das redes
/teste     /#teste     301
/planos    /#planos    301

# ⚠️ NÃO recriar aqui a regra curinga que existia na Netlify:
#
#     /*  /index.html  200
#
# A Cloudflare a REJEITA e derruba o deploy inteiro:
#   "Line 14: Infinite loop detected in this rule. This would cause a redirect
#    to strip `.html` or `/index` and end up triggering this rule again."
#
# Ela já serve o index.html na raiz, então a regra se retroalimenta. O mesmo
# efeito (rota inexistente cai na home) vem de `not_found_handling` no
# wrangler.jsonc — que já está configurado. Não precisa das duas.
